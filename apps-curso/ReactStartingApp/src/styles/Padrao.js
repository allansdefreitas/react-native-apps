/* My own custom style for components! */
import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    exercise: {
        paddingHorizontal: 15,
        marginVertical: 6,
        borderRadius: 10,
        borderWidth: 2,
        borderColor: '#222', // gray
        fontSize: 24,
        fontWeight: 'bold'
    }

})